{
    "name": "@tlock/shared",
    "version": "1.0.0",
    "description": "Shared utilities for Tlock 2FA wallet",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "typescript": "^5.7.3"
    },
    "files": [
        "dist"
    ],
    "dependencies": {
        "@noble/curves": "^1.9.2",
        "@noble/hashes": "^1.8.0"
    }
}
