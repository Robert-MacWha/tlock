{
    "name": "@tlock/shared",
    "version": "1.0.0",
    "description": "Shared utilities for Tlock 2FA wallet",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist",
        "test": "jest",
        "lint": "eslint . --cache --ext js,ts"
    },
    "devDependencies": {
        "typescript": "^5.7.3"
    },
    "files": [
        "dist"
    ],
    "dependencies": {
        "jest": "^30.0.4",
        "viem": "^2.33.1"
    }
}
